[project]
name = "langchain-abstraction-tax"
version = "0.1.0"
description = "Benchmarking LangChain against leaner RAG orchestration alternatives"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
