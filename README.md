# langchain-tax-abstraction
Benchmarking LangChain against LlamaIndex, DSPy, plain function-calling, and raw API calls on the same RAG task ; same LLM, same retrieval, only the orchestration layer changes. Measures retrieval quality, answer correctness, latency, cost, and code complexity to answer: what does the abstraction actually buy you?
